# Design Feedback

> Generated by **Refine AI** · 2026-03-28 01:57:13 UTC

## Instructions

This file contains design feedback captured by a designer reviewing the app. Each issue is a visual or UX problem that needs to be fixed in the codebase.

**Work through every issue below, top to bottom (already sorted by priority — 🔴 High first).**

For each issue:

1. Read the feedback and study the screenshot carefully — it shows the exact screen where the problem appears
2. Use the `Component`, `File`, and `Suggested fix` fields to locate the right code
3. Implement the fix
4. If the Refine AI MCP server is connected, call `resolve_annotation` with the issue `ID` to mark it done

Field reference:

| Field | What it means |
|---|---|
| `ID` | Unique annotation ID — pass to `resolve_annotation` when fixed |
| `Component` | The React component where the issue appears |
| `File` | Source file path (if detected) |
| `Route/Page` | Screen or route the issue was captured on |
| `Tags` | Category of the issue: `spacing`, `typography`, `color`, `layout`, `interaction`, or `copy` — use to group related fixes |
| `Feedback` | What the designer observed and wants changed |
| `Suggested fix` | Specific code change recommended — use this as your implementation guide |
| `Location` | Pixel coordinates on screen where the designer tapped — helps pinpoint the element |
| Screenshot | Visual reference of the screen — look at it before making any changes |

---

## Summary

| Priority | Count |
|----------|-------|
| 🔴 High  | 0 |
| 🟡 Medium | 0 |
| 🟢 Low   | 0 |
| **Total** | **0** |

---

## ✅ All caught up — no pending annotations!